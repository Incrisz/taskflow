import {Router} from 'express'; import {listTasks,getTask,createTask,updateTask,deleteTask} from '../controllers/task.controller'; const r=Router();r.get('/',listTasks);r.get('/:id',getTask);r.post('/',createTask);r.put('/:id',updateTask);r.delete('/:id',deleteTask);export default r;
