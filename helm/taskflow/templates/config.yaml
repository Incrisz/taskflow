apiVersion: v1
kind: ConfigMap
metadata:
  name: taskflow-config
  namespace: {{ .Release.Namespace }}
data:
  PORT: '5000'
  DB_HOST: postgres
  DB_PORT: '5432'
  DB_NAME: {{ .Values.config.databaseName | quote }}
  REDIS_HOST: redis
  REDIS_PORT: '6379'
