apiVersion: v1
kind: ConfigMap
metadata:
  name: taskflow-backend-config
  namespace: taskflow

data:
  PORT: "5000"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "taskflow"
  DB_USER: "taskflow"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"