apiVersion: v1

kind: Service

metadata:

  name: taskflow-frontend

  namespace: taskflow


spec:

  type: NodePort


  selector:

    app: taskflow-frontend


  ports:

  - port: 80

    targetPort: 80

    nodePort: 30080